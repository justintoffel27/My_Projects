#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string first = get_string("What's your first name? "); //Insert name//
    printf("hello, %s\n", first); //%s is "plug in some value here// //SO it will say "hello, "name""
}
