#include <stdio.h>

int main(void)
{
    char s[4];              // Integer 'string'
    printf("s: ");          // Prompt
    scanf("%s", s);         // Scanf scans the entire keyboard for any input    // %s is an integer
    printf("s: %s\n", s);   // Prints
}
